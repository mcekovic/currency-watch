<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<parent>
		<groupId>org.strangeforest.currencywatch</groupId>
		<artifactId>currency-watch</artifactId>
		<version>1.3.3-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>

	<artifactId>currency-watch-it-reports</artifactId>
	<packaging>pom</packaging>
	<name>Currency Watch IT Reports</name>

	<profiles>
		<profile>
			<id>integration-tests</id>
			<properties>
				<project.parent.basedir>${project.basedir}/..</project.parent.basedir>
			</properties>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<artifactId>maven-antrun-plugin</artifactId>
							<dependencies>
								<dependency>
									<groupId>org.jacoco</groupId>
									<artifactId>org.jacoco.ant</artifactId>
									<version>${jacoco-maven-plugin.version}</version>
								</dependency>
							</dependencies>
						</plugin>
					</plugins>
				</pluginManagement>
				<plugins>
					<plugin>
						<artifactId>maven-antrun-plugin</artifactId>
						<executions>
							<execution>
								<id>merge-jacoco-coverage</id>
								<phase>post-integration-test</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target xmlns:jacoco="antlib:org.jacoco.ant">
										<taskdef uri="antlib:org.jacoco.ant" resource="org/jacoco/ant/antlib.xml"/>
										<jacoco:merge destfile="${project.parent.basedir}/target/jacoco.exec">
											<fileset dir="${project.parent.basedir}">
												<include name="core/target/*.exec"/>
												<include name="app/target/*.exec"/>
												<include name="web/target/*.exec"/>
											</fileset>
										</jacoco:merge>
									</target>
								</configuration>
							</execution>
							<execution>
								<id>report-jacoco-coverage</id>
								<phase>verify</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target xmlns:jacoco="antlib:org.jacoco.ant">
										<taskdef uri="antlib:org.jacoco.ant" resource="org/jacoco/ant/antlib.xml"/>
										<jacoco:report>
											<executiondata>
												<file file="${project.parent.basedir}/target/jacoco.exec"/>
											</executiondata>
											<structure name="Currency Watch">
												<classfiles>
													<fileset dir="${project.parent.basedir}/core/target/classes"/>
													<fileset dir="${project.parent.basedir}/app/target/classes"/>
													<fileset dir="${project.parent.basedir}/web/target/classes"/>
												</classfiles>
												<sourcefiles encoding="UTF-8">
													<fileset dir="${project.parent.basedir}/core/src/main/java"/>
													<fileset dir="${project.parent.basedir}/app/src/main/java"/>
													<fileset dir="${project.parent.basedir}/web/src/main/java"/>
												</sourcefiles>
											</structure>
											<html destdir="${project.parent.basedir}/target/site/jacoco"/>
										</jacoco:report>
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
